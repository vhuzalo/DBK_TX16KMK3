## Melhorias

- Campo `BatAlertPct` alterado para entrada numérica, facilitando o ajuste pelo scroller.
- Nome do piloto removido das opções do widget e migrado para arquivo de configuração no SD.
- Novo arquivo `/WIDGETS/DBK_TX16KMK3_config.json` para persistir configurações fora da pasta do widget.
- Suporte às chaves `pilot_name`, `battery_alert_pct` e `battery_alert_interval`.
- Criação automática do arquivo de configuração com valores padrão quando ele não existir ou estiver vazio.
- Recarga dinâmica da configuração em tempo de execução, refletindo mudanças no JSON sem recriar o widget.
- Lógica de configuração extraída de `main.lua` para `config.lua`, deixando o código principal mais limpo.
- Workflow de release ajustada para empacotar `config.lua` e a pasta `audio`.
