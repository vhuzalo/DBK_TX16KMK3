## Correcoes

- Corrigido o alerta de bateria baixa para respeitar o valor persistido em `BatAlertPct` nas opcoes do widget.
- Removida a sobrescrita em runtime do percentual de alerta com `battery_alert_pct` do JSON.
- Ajustado o comportamento para manter o JSON apenas como valor padrao inicial da opcao do widget.
